# Optional ML stack for model-backed embeddings/captions.
# CPU wheels are used on Linux to avoid massive CUDA downloads.
--extra-index-url https://download.pytorch.org/whl/cpu
sentence-transformers>=3.0.1
transformers>=4.44.0
torch==2.4.1+cpu; platform_system == "Linux"
torchvision==0.19.1+cpu; platform_system == "Linux"
torch>=2.4.1; platform_system != "Linux"
torchvision>=0.19.1; platform_system != "Linux"
